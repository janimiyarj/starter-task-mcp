# Playwright MCP Artifacts

This folder stores Playwright MCP run artifacts.

- `output/` contains traces and session data from MCP-driven browser runs.
- You can safely delete files in `output/` to clean old runs.
